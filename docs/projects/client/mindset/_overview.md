# mindset/ (client)

Mental frameworks for analysing problems specific to the client's async multi-storage architecture.

## Entries

- [`severity-from-invariants.md`](severity-from-invariants.md) — assess severity from violated invariants (writes lost, ordering broken, dependencies cascaded) rather than from UI symptoms or who reports the bug. Surfaced in the project's `Always Read entries`.
